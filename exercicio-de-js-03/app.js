// Defina o saldo da conta
let saldoConta = 1000; // Exemplo de saldo

// Crie uma mensagem usando uma template string para incluir o valor do saldo
let mensagemSaldo = `Seu saldo atual é de R$ ${saldoConta.toFixed(2)}.`;

// Exiba a mensagem para o usuário
alert(mensagemSaldo);
